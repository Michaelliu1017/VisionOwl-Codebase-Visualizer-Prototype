# VisionOwl 执行方案

## 一、What：要做什么

VisionOwl 是一个本地优先的交互式代码知识工作台。用户导入代码仓库后，系统生成带源码证据的模块图谱，并把模块详情、上下游关系、团队文档、批注和 AI 问答组织在同一界面中。

产品不再承担线上监控职责，也不展示虚构的实时状态。原来的运行拓扑改为代码路径推演：用户选择一个模块后，系统根据源码中能够验证的依赖关系，播放该模块可能参与的交互路径。

第一阶段包含以下能力：

1. 导入本地代码仓库并生成模块图谱。
2. 展示模块、包含关系和跨模块依赖。
3. 每条关系保留文件、行号和代码片段证据。
4. 点击模块查看详情、上下游、文档与批注。
5. 在选中模块的上下文中与 Codex 对话。
6. 以动画形式推演源码支持的可能交互路径。
7. 使用 SQLite 保存项目、图谱版本、文档、批注和会话。
8. 使用 Electron 提供本地桌面应用和目录选择。

## 二、Why：解决什么问题

复杂代码库存在几个常见痛点：

- 新成员很难快速理解模块边界和调用关系。
- 普通架构图与代码脱节，代码变化后很快过期。
- AI 给出的关系缺少文件和行号证据，难以判断是否可信。
- 文档、批注和源码分散，无法围绕具体模块共同维护。
- 静态图只能展示结构，无法直观表达一次业务链路可能怎样经过不同模块。

VisionOwl 将源码事实作为底座，AI 只补充语义和解释，不允许凭空增加关系。路径动画展示的是有证据的代码交互推演，不冒充线上真实调用。

## 三、How：怎么实现

### 3.1 整体链路

代码仓库 → 只读扫描器 → 模块与关系图谱 → SQLite 图谱版本 → 路径推演引擎 → React Flow 可视化 → 模块上下文 AI

### 3.2 核心模块

| 模块 | 职责 |
|---|---|
| Repository Scanner | 遍历代码、识别模块、解析 import 和本地包依赖 |
| Evidence Graph | 保存实体、关系及对应文件、行号和代码片段 |
| Analysis Service | 管理扫描任务、进度事件、版本发布和可选 Codex 增强 |
| Path Simulation Engine | 从选中模块构造有源码证据的上下游交互序列 |
| Knowledge Store | 使用 SQLite 保存项目、图谱、文档、批注和对话 |
| Code Workspace | 展示全景、聚焦和路径推演三种模式 |
| Context Chat | 将选中模块、关系、文档和批注作为 Codex 上下文 |
| Electron Shell | 启动本地后端并安全地提供目录选择 |

### 3.3 三种图谱模式

| 模式 | 表现 |
|---|---|
| 系统全景 | 展示全部模块和关系，选中节点不会隐藏其他模块 |
| 模块聚焦 | 突出选中模块及直接上下游，显示文档和批注玻璃浮层 |
| 路径推演 | 按证据顺序逐段点亮节点和连线，其他关系降暗但不删除 |

三种模式复用同一份图谱和节点位置，切换模式不会重新生成数据，也不会造成图谱跳动。

### 3.4 路径推演规则

1. 排除仅表示项目包含模块的 contains 关系。
2. 只采用至少包含一条源码证据的跨模块关系。
3. 优先采用证据数量和源码引用次数较多的关系。
4. 从选中模块向上寻找调用来源，再向下遍历依赖模块。
5. 限制单次推演长度，避免复杂仓库产生不可读动画。
6. 根据路径长度动态调整速度，整次播放控制在四秒内。
7. 当前关系显示对应文件和行号。
8. 推演完成后保留路径高亮，用户可单击原节点或重播按钮再次播放。

### 3.5 AI 的职责

Codex 负责：

- 补充模块职责、标签和业务含义。
- 回答当前模块相关问题。
- 解释源码证据和可能的影响范围。
- 后续根据用户描述选择适合的业务路径。

Codex 不负责：

- 凭空创建模块或调用关系。
- 把静态依赖描述成线上真实调用。
- 生成未经观测的耗时、QPS、错误率或健康状态。

### 3.6 UI 约束

- 使用黑色背景、深灰模块和绿色强调色。
- 模块、文档和批注保持半透明磨砂玻璃风格。
- 选中模块使用半透明绿色填充。
- 有关连线变为绿色，无关内容只降低亮度。
- 推演中的当前连线使用连续流光，当前节点轻微放大并变亮。
- 动画完成后节点恢复正常尺寸并保留路径状态。
- 不让连线覆盖有关模块的内容，不因选中状态删除边。
- 关闭减少动画系统设置时，所有动画自动降级。

## 四、验收标准

1. 不启用任何运行监控插件也能完整启动。
2. 主界面不再显示运行拓扑、M5 或拨测监控入口。
3. 三种模式共享同一份静态代码图谱。
4. 选择模块后可以单击切换其他模块，也可以取消选择。
5. 聚焦模式不会出现连线消失或穿透玻璃卡片的问题。
6. 推演模式每次仅播放当前一段流光，并在四秒内完成。
7. 每段推演关系都能对应至少一条源码证据。
8. 390 像素宽度下页面不产生横向溢出。
9. 后端测试、TypeScript 检查和前端生产构建全部通过。

## 五、后续迭代

- 从 import 依赖扩展到函数调用、HTTP 路由、RPC、数据库访问和消息队列。
- 允许用户输入业务场景，由 Agent 在证据图中选择候选路径。
- 支持本地运行单元测试或接口测试，记录本次真实执行路径。
- 接入钉钉文档同步和 Aone 仓库。
- 增加人工修正关系、审核 AI 解释和图谱版本对比。
