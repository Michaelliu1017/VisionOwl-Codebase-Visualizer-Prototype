# Repository Insights

`@visionowl/repository-insights` turns a VisionOwl graph snapshot into a small,
source-free summary for dashboards and reports. Its public entry point delegates
graph aggregation to `graph-summary.js`, giving scanners a concrete module and
an internal call relationship to discover.
