# VisionOwl 架构总览审计与改造方案

## 一、当前问题

### 1. 架构内容不完整

- 总览只识别出 GoProbe、Agent-Rest、TaskCenter 三个代码域，遗漏了 Worker。
- Redis、MySQL、SLS 等真实依赖没有成为独立架构节点，无法看清数据存储和结果写入链路。
- 一个代码域内的模块名称过于抽象，例如“工程配置与分析元数据”，不能直接对应真实业务职责。
- 同一模块混入多个仓库的文件，导致模块边界与部署边界不一致。
- 静态依赖、运行时调用、数据读写和任务流转没有明确区分。

### 2. 主流程不清楚

- 页面没有给出明确的阅读起点、终点和主方向。
- 任务创建、调度、下发、执行、回传和写入 SLS 的主链路没有形成连续路径。
- 大量次要依赖与主流程使用相同的视觉权重，主链路被淹没。
- 连线默认缺少关系名称，用户必须点击后才能猜测含义。

### 3. 图布局混乱

- 三个代码域被排成不稳定的三角形，不能表达系统的处理顺序。
- 跨域连线大量交叉、折返、重叠，部分线穿过无关模块。
- 连线在代码域边界和模块边缘附近聚集，端点难以辨认。
- 模块间距不足，连线标签没有稳定的展示空间。
- 画布存在明显空洞，但中心区域仍然拥挤，空间利用不均衡。

### 4. 视觉层级不足

- 所有代码域都使用高亮绿色边框，容易被误解为全部处于选中状态。
- 代码域、代码模块、基础设施和外部系统缺少清晰的颜色与外观区分。
- 节点副标题被截断，无法起到快速说明用途的作用。
- 图标语义重复，不能帮助识别服务、执行模块、存储和日志系统。
- 暗色玻璃效果存在，但信息层级仍主要依赖边框，整体辨识度不足。

### 5. 交互和证据不足

- 总览态没有图例，用户不知道颜色、线型和箭头分别代表什么。
- 缺少“只看主流程”“展开细节”“查看数据写入链路”等分层入口。
- 点击节点后虽然可以分析，但总览中缺少代码证据和关系证据提示。
- 图谱结果没有明显说明来自静态代码事实、配置事实还是 AI 归纳。

## 二、根因与责任边界

| 问题 | 主要根因 | 应修改位置 |
| --- | --- | --- |
| Worker 缺失、多个仓库混入同一模块 | 社区发现结果跨仓库合并，后端没有先保护仓库边界 | 后端分组算法为主，分析 Skill 补充约束 |
| Redis、MySQL、SLS 缺失 | 分析规则只关注代码模块，基础设施事实没有升级为架构节点 | 分析 Skill 与后端图谱适配器 |
| 模块名称抽象且不稳定 | AI 在混合文件集合上总结，输入边界本身不准确 | 后端先修边界，分析 Skill 再约束命名 |
| 静态依赖与业务链路混在一起 | 关系没有按语义分层 | 分析 Skill、关系模型和前端展示策略 |
| 连线交叉、折返、遮挡 | 前端以紧凑面积为优先，并在总览态绘制过多精确边 | 前端布局与连线代码 |
| 总览态看不到关系含义 | 布局 Skill 和代码默认隐藏背景关系标签 | 布局 Skill 与前端标签策略 |
| 所有大框都像选中状态 | 默认视觉样式没有区分容器边界与选中态 | 前端样式 |

结论：这不是单独优化 Skill 或单独调整 CSS 能解决的问题，必须同时修改 Skill、后端建模和前端渲染。

## 三、改造方案

### 1. 分析与建模

- 多仓库目录先按真实 Git 仓库边界拆分，再在每个仓库内部识别模块，禁止一个模块跨越两个独立仓库。
- 每个代码域优先使用仓库或可部署服务名称，内部模块使用“职责名称 + 代码证据”生成。
- 从配置、客户端初始化和读写调用中提取 Redis、MySQL、SLS 等确定性依赖；没有代码证据时不创建节点。
- 将关系分为静态依赖、运行时调用、任务流转、数据读写和结果上报，避免统一显示为模糊的 depends_on。
- 保存关系来源文件和证据摘要，供点击分析时引用。

### 2. 总览与聚焦

- 总览态首先展示代码域、基础设施和主流程，不直接铺满所有跨域精确连线。
- 同一对代码域之间的多条跨域关系在总览态聚合为一条带名称和数量的主干线。
- 选中代码域或模块后，再展开精确到模块端点的关系。
- 主流程关系持续显示简短标签；次要关系在选中或悬停时显示。
- 静态依赖使用低亮度实线，数据读写使用有方向的虚线，运行时主链路使用高亮脉冲线。

### 3. 布局

- 总览优先采用从左到右的分层布局，遵循“创建 → 调度 → 下发 → 执行 → 回传/存储”的阅读方向。
- 代码域按关系层级排列，不再仅以占地面积最小作为布局目标。
- Redis、MySQL、SLS 放在对应读写链路的邻近数据层，避免跨越全部代码域。
- 为节点标题、关系标签和并行连线预留固定间距。
- 循环依赖和双向关系使用并行轨道或合并标记，避免两条线重叠。

### 4. 视觉

- 绿色细边框表示普通代码域边界，绿色填充和泛光只用于选中态。
- 代码模块保持深灰玻璃；基础设施使用不同图标和较冷的中性色玻璃。
- 主流程、告警和选中关系分别使用绿色、橙色和红色，其他关系降低亮度。
- 节点副标题只展示一句可读的职责说明，不展示目录名或重复标签。

## 四、验收标准

- develop-repo 中的 GoProbe、Agent-Rest、TaskCenter、Worker 至少分别形成独立代码域。
- 任何架构模块都不得同时包含两个独立 Git 仓库的文件。
- 有确定代码证据时，Redis、MySQL、SLS 能作为独立依赖节点出现。
- 首屏能看出系统主方向，跨域主干线有关系名称且不穿过无关节点。
- 总览态的跨域线数量显著下降，选中后仍能查看精确模块关系。
- 节点副标题不再显示目录或抽象元数据，而是说明模块用途。
- 普通、选中、基础设施和异常状态在视觉上可以直接区分。
